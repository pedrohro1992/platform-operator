---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: pgdatabases.database.platform.io
spec:
  group: database.platform.io
  names:
    kind: PGDatabase
    listKind: PGDatabaseList
    plural: pgdatabases
    singular: pgdatabase
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PGDatabase is the Schema for the pgdatabases API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PGDatabaseSpec defines the desired state of PGDatabase
            properties:
              instances:
                default: 3
                format: int32
                minimum: 1
                type: integer
              storageSize:
                description: Storage size, e.g., "1Gi"
                type: string
              version:
                default: "15"
                description: Postgres version (Major version, e.g., "15")
                type: string
            required:
            - storageSize
            type: object
          status:
            description: PGDatabaseStatus defines the observed state of PGDatabase
            properties:
              phase:
                description: Status of the underlying CNPG Cluster
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
